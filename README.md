# Bouncer AI

An AI-powered bouncer system built with TATTVA.

## Setup

1. Clone the repo
2. Install dependencies: `npm install`
3. Copy `.env.example` to `.env` and fill in your values
4. Run: `npm start`

## Usage

Configure your settings in `.env` and start the application. The AI bouncer will handle access control automatically.
